version: "2"
sql:
    - engine: "postgresql"
      queries: "pkg/db/queries.sql"
      schema: "pkg/db/migrations"
      gen:
          go:
              out: "pkg/db"
              package: "db"
              sql_package: "pgx/v5"
              emit_json_tags: true
