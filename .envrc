#!/usr/bin/env bash
# Used by https://direnv.net

# Automatically reload when this file changes
watch_file nix/devshell.nix

# Load `nix develop`
use flake

PRJ_ROOT=$(git rev-parse --show-toplevel)
PRJ_DATA_DIR="$PRJ_ROOT/.data"

export PRJ_ROOT PRJ_DATA_DIR

mkdir -p "$PRJ_DATA_DIR"

PGDATA="$PRJ_DATA_DIR/postgres"
PGHOST="$PRJ_DATA_DIR"
PGPORT="5434"
PGLISTEN="127.0.0.1"
PGUSER="narwal"
PGPASS="!Pa55w0rd"
PGDATABASE="narwal_dev"

DATABASE_URL="postgres://$PGUSER:$PGPASS@localhost:$PGPORT/$PGDATABASE?sslmode=disable"

NARWAL_POSTGRES_URL="$DATABASE_URL"

export PGDATA PGHOST PGPORT PGLISTEN PGUSER PGPASS PGDATABASE DATABASE_URL NARWAL_POSTGRES_URL

NARWAL_S3_SSL_ENABLED="false"
NARWAL_S3_ENDPOINT="127.0.0.1:4566"
NARWAL_S3_CREDENTIALS_ACCESS_KEY_ID="test"
NARWAL_S3_CREDENTIALS_SECRET_ACCESS_KEY="test"
NARWAL_S3_BUCKET="narwal-dev"
NARWAL_SQS_UPLOAD_EVENT_QUEUE="narwal-s3-upload-event"
NARWAL_SQS_DELETE_EVENT_QUEUE="narwal-s3-delete-event"

echo "$MINIO_ROOT_USER" > "$PRJ_DATA_DIR/s3_access_key"
echo "$MINIO_ROOT_PASSWORD" > "$PRJ_DATA_DIR/s3_secret_key"

export NARWAL_AWS_SSL_ENABLED NARWAL_AWS_ENDPOINT
export NARWAL_AWS_CREDENTIALS_ACCESS_KEY_ID NARWAL_AWS_CREDENTIALS_SECRET_ACCESS_KEY
export NARWAL_S3_BUCKET NARWAL_SQS_UPLOAD_EVENT_QUEUE NARWAL_SQS_DELETE_EVENT_QUEUE

GOOSE_DRIVER=postgres
GOOSE_DBSTRING="$DATABASE_URL"
GOOSE_MIGRATION_DIR=pkg/db/migrations

export GOOSE_DRIVER GOOSE_MIGRATION_DIR GOOSE_DBSTRING

# Used to poke at the NixOS cache. Login with `aws sso login`
export AWS_CONFIG_FILE=$PWD/nixos-infra/aws-config
export AWS_PROFILE=nixos-archeologist

# Extend the environment with per-user overrides
source_env_if_exists .envrc.local
