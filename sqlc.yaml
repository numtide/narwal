version: "2"
sql:
    - engine: "postgresql"
      queries: "pkg/hydra/queries.sql"
      schema: "pkg/hydra/hydra.sql"
      gen:
          go:
              out: "pkg/hydra"
              package: "hydra"
              sql_package: "pgx/v5"
              emit_json_tags: true
