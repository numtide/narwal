version: "2"
sql:
    - engine: "postgresql"
      queries: "pkg/queries/queries.sql"
      schema: "pkg/queries/hydra.sql"
      gen:
          go:
              out: "pkg/queries"
              package: "queries"
              sql_package: "pgx/v5"
              emit_json_tags: true
